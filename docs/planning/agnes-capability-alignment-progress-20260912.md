# Agnes 能力边界对齐进度

- [x] 读取现有 Agnes 真实验收记录和官方协议证据，确认本阶段不重新真实生成。
- [x] 冷保存计划与反方审查。
- [x] Agnes 适配器边界校验与归一化。
- [x] 视频设置面板及参考图入口对齐当前模型。
- [x] 定向回归 2/2 通过：超限素材不发 POST，旧配置按 Flash 合法范围发送。
- [x] 完整浏览器回归 36/36 通过，单 worker、无自动重试。
- [x] Web 类型检查与生产构建通过。
- [x] Docs 类型检查通过；文档、变更日志和现有测试截图已更新/保留；差异检查通过。

## 本阶段证据

- 官方协议：Agnes Video 2.5 Flash 固定 720P，支持 4–12 秒，reference 图片最多 5 张、音频最多 3 段，不支持参考视频。
- 代码：`web/src/services/api/agnes.ts` 集中校验并归一化；`web/src/components/video-settings-panel.tsx` 和视频工作台按 Agnes 模型收窄界面。
- 真实调用：沿用 2026-09-11 的独立串行实测，不重复生成；文本、图片和两段视频结果仍在 `D:\codex\.local\agnes-validation-20260911`。
- 残余边界：常用浏览器仍需用户自行填入 API Key；本地视频参考素材仍没有用户授权的公开上传服务；视频独立任务仓储、跨标签协调和全站 UI 对齐继续列为后续工作。

当前工作树：`D:\codex\HuaBuInfiniteCanvas`。保留上一阶段所有未提交改动，不回滚用户改动。

## 最终审计

- Web 构建仅有既有的大包分块和动态/静态导入提示，没有构建失败。
- Docs 构建和类型检查通过；仅有 `llms.txt` 路由的 Turbopack 文件追踪提示。
- 凭据扫描未发现真实密钥，仅命中既有脱敏测试假值。
- 未提交、未推送 Git；工作树中的上一阶段改动按用户要求保留。
