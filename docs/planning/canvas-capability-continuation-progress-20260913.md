# 画布能力续作进度

## 已完成

- 新增计划与对抗审查，明确以真实主流程能力为验收标准。
- 修正 Config 主流程的真实方向验证：资源、文本和 Group 通过入边进入 Config，Config 作为生成目标读取这些输入。
- 参考栏来源增加真实连接目标信息，聚合输入移除时不再错误断开执行目标的另一条边。
- 保留对导入数据中 Config -> 目标方向的兼容读取和去重测试。
- 修复插件文本生成漏传 `options.references` 的主入口问题。
- 修复文本任务新上传参考图使用 `blob:` URL 发给 Provider 的问题；本地任务继续保存 `storageKey`，请求体使用 Data URL。
- 增加插件主入口多模态请求体测试，证明安装的插件通过 `ctx.ai.generateText` 传入的参考图进入真实 `/responses` 请求。
- 修复文本任务取消状态依赖 Provider 及时响应的问题；取消信号触发后立即持久化 `interrupted`，并阻止迟到成功结果覆盖中断状态。

## 当前证据

- `npm run typecheck` 通过。
- 全量 Playwright `72/72` 通过。
- 生产构建通过；仅保留既有的大 bundle 和动态导入提示。
- Config/Group/视频专项测试通过。
- 插件文本参考专项首次暴露了 `blob:` 请求体问题，修复后专项通过。
- 插件文本中断用例连续 5 次通过，确认 partial 内容保留且只提交一次。
- 用户原有规划截图仍未纳入改动。

## 待继续

- 对插件图片/视频、画布重试和任务中心逐入口检查“参数声明但主流程漏传”的同类问题。
- 继续做当前浏览器中的视觉和手动交互验收。
