# 卡藏对齐调研恢复点

日期：2026-09-11

## 不变的任务目标

在 `D:\codex\HuaBuInfiniteCanvas` 的真实 infinite-canvas 源码底座上继续开发，迁移 HuaBu 已验证的能力并重新设计 UI，对齐卡藏实际工作流。不能恢复 Nova 作为运行核心，也不能仅安装上游后宣称迁移完成。

## 最新用户信息

用户已登录卡藏画布。此信息来自用户，不再把之前独立 Chromium 的未登录结果当成用户当前状态。

## 浏览器连接检查

- `cua.listBrowsers()` 能列出 Edge 扩展浏览器和当前任务内置浏览器。
- 当前任务内置浏览器没有标签页。
- Edge 的标签页枚举及会话命名操作被工具认证错误阻断：`unsupported Codex auth method: apikey`。
- 尚未取得已登录卡藏标签页的 DOM 或截图。
- 没有重新登录、读取凭据、导出 cookies、修改远端项目或提交生成。
- 不再重复创建没有登录态的独立浏览器来推断用户登录状态。

## 上次调研的证据边界

- `kachang-reference-1440.png` 是之前独立 Chromium 中的登录页，不是内部工作画布截图。
- 公共资源可提供界面结构线索，但不能代替已登录状态的操作验收。
- 之前查看 `canvas-after-fixes.png` 时已发现：390px 虽无 body 横向滚动，左面板仍挤占画布且顶部控件重叠。因此旧验收表的“移动端宽度通过”不代表移动端可用，下一轮必须验证可见性、可点击性和遮挡。
- 上轮仅完成研究和本地代码读取，没有实施卡藏对齐 UI、业务能力迁移或新的对齐计划。

## 后续执行顺序

1. 浏览器连接可用后，只读检查已登录画布；连接不可用时使用用户提供的实际画布及展开面板截图。
2. 冷保存完整对齐计划、已验证能力迁移矩阵和对抗审查。
3. 在上游组件、项目 store、节点注册表、生成服务与素材存储边界实施；不复制第三方闭源代码或品牌。
4. 单独验证桌面、390px、展开面板、连线正反向、菜单边缘定位、即时创建、保存恢复和真实执行边界。

当前完成的是连接诊断与恢复记录，不是卡藏对齐工程完成。
