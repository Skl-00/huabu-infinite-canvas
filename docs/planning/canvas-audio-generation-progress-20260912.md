# 画布音频持久生成进度

- [x] 读取现有图片/视频任务仓储、音频 API、媒体存储和画布音频入口。
- [x] 保存音频阶段计划并完成反方审查。
- [x] 完成 `AudioRun` 类型、仓储和执行器。
- [x] 将画布新建、重试和结果回填接入 `AudioRun`。
- [x] 完成媒体清理、场景复制和历史节点兼容边界。
- [x] 完成定向测试、全量回归、构建和最终验收文档。

当前工作树：`D:\codex\HuaBuInfiniteCanvas`。继续保留所有既有未提交改动，不回滚用户或前序阶段改动。

## 本阶段边界

- 文本流式生成和插件自定义协议暂不迁移。
- 音频请求保持单请求串行，不扩大免费渠道并发。
- 不在任务记录中保存 API Key 或完整画布快照。

## 本阶段验收记录

- `npm run typecheck`：通过。
- `npm run build`：通过；仅有既有动态导入和大 chunk 警告。
- `docs/npm run types:check`：通过。
- `git diff --check`：通过；仅有 Windows 工作树的 LF/CRLF 提示。
- `npm test`：`47/47` 通过（2026-09-12）。
- 音频定向链路已验证：配置节点生成、任务提交前落盘、`audio:*` 媒体保存、结果回填、刷新恢复、凭据不进入任务记录。

## 对抗性结论

- Provider 请求失败不会静默重提；重试会创建子任务。
- 页面刷新或失去任务锁时不会猜测远端音频已成功，只有已暂存 Blob 才允许继续本地保存。
- 节点删除不会删除独立音频任务；场景复制会清除运行身份但保留已有媒体。
- 当前仍未完成：文本流式生成迁移、插件统一协议、云同步/备份和 GitHub 仓库发布。
