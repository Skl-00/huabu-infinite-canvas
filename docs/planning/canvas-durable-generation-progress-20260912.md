# 画布持久生成接入进度

- [x] 读取视频任务阶段、画布生成入口、节点元数据和场景复制边界。
- [x] 冷保存计划并完成反方审查。
- [x] 扩展任务上下文并接入画布图片/视频执行。
- [x] 接入场景打开后的结果回填与节点生命周期边界。
- [x] 完成画布图片/视频主链、刷新、复制身份清理和任务保留测试。
- [x] 完成类型检查、完整回归、构建和验收文档。

当前工作树：`D:\codex\HuaBuInfiniteCanvas`。继续保留所有既有未提交改动，不回滚用户或前序阶段改动。

## 当前验收

- Web 类型检查通过。
- Web 全量 Playwright 回归通过：`46/46`。
- Web 生产构建通过；仅保留既有动态导入与大 chunk 警告。
- Docs 类型检查通过：`npm run types:check`。
- `git diff --check` 通过；仅有 Windows 工作树的 LF/CRLF 提示。
- 画布图片主生成定向回归通过：任务记录含项目/场景/节点上下文，结果回填节点，刷新后恢复。
- 画布视频主生成定向回归通过：远端任务 ID 只提交一次，完成媒体写入任务仓储并回填节点，刷新后不重复 POST。
- 场景复制会移除 `generationRunId` / `generationRunKind`，保留已有媒体内容但不复制执行身份。

## 对抗性验收结论

- 画布节点删除不会删除独立任务记录；任务结果可继续在任务中心查看。
- 页面刷新后只要目标节点仍存在，已完成或失败的图片/视频结果会重新投影到节点。
- 场景复制不会继承旧生成任务的运行身份，避免复制场景后误回写原场景。
- 任务上下文只保存项目、场景和节点 ID，不保存 Provider 密钥、完整画布快照或请求凭据。
- 现阶段仅迁移画布图片/视频生成；画布文本、音频、插件生成仍保留旧路径，不能宣称已完成统一任务仓储迁移。
- 构建警告和剩余能力差距已记录为后续工作，不影响本阶段可验证主链。
