# 视频任务仓储与跨标签协调进度

- [x] 读取上一阶段 Agnes 实测、协议适配和现有图片任务仓储实现。
- [x] 冷保存计划并完成反方审查。
- [x] 建立 VideoRun 类型与本地仓储。
- [x] 接入视频执行器和跨标签锁。
- [x] 接入视频工作台与任务中心；首页、Agent 状态与素材清理同步接线。
- [x] 完成定向/完整回归、构建和验收文档。

当前工作树：`D:\codex\HuaBuInfiniteCanvas`。保留上一阶段未提交改动，不回滚或覆盖用户变更。

实现已落地。新任务仅写 `video_runs`；旧历史保留可读、不自动查询或重新生成。

## 首轮验收

- Web 类型检查通过。
- Agnes 与新视频任务定向回归 19/19 通过，无自动重试。
- 已确认双标签单查询、刷新只 GET、未知提交不重发、冻结重试与子任务去重、渠道变化阻断、两类仓储写入失败恢复、资产清理引用保留和 Agent 持久状态。
- 六张桌面/手机明暗截图已产出。审阅后缩减已完成任务的元数据占屏，视频预览限制桌面高度，短屏保留可展开的任务信息。
- 新增媒体文件写入故障后跨刷新重新保存，以及查询密钥轮换/渠道变化的回归；待完整测试确认。

## 最终验证

- `npm run typecheck`：通过。
- `npm test`：44/44 通过，单 worker、无自动重试。
- `npm run build`：通过；仅有既有大包与动态导入提示。
- `cd docs && npm run types:check`：通过。
- `git diff --check`：通过；未提交、未推送，保留工作树中此前的未提交改动。
