# 音频任务中心进度

- [x] 保存计划并完成反方审查。
- [x] 新增音频任务列表、详情、筛选和搜索。
- [x] 接入刷新、失败重试、结果保存和中断任务核对。
- [x] 接入 `/tasks` 的图片/视频/音频切换与 URL 任务定位。
- [x] 增加画布音频生成到任务中心的浏览器验收。
- [x] 完成类型检查、定向测试、全量回归、构建和差异审计。

## 验收

- 定向音频 E2E：成功查看/刷新 `1 passed`；失败重试故障注入 `1 passed`。
- 本阶段全量 Playwright：`47/47 passed`；文本阶段完成后的当前工作树回归为 `50/50 passed`。
- Web `npm run typecheck`：通过。
- Web `npm run build`：通过；仅有既有动态导入和大 chunk 警告。
- `git diff --check`：通过；仅有 Windows 工作树的换行提示。

## 边界

- 任务中心不复制 Provider 请求逻辑，所有生成动作复用 `audio-runner`。
- 没有远端续查协议的 `interrupted` 音频不会被伪装成成功；只允许核对或保存已暂存结果。
- 本轮浏览器证据覆盖成功生成、任务中心查看、播放器、刷新恢复，以及故障注入失败后创建子任务并成功重试；暂存结果恢复按钮已接线，仍需专门模拟 Blob 保存中断的 E2E。
- 文本流式生成已在后续阶段迁移；插件统一协议、云同步/备份和 GitHub 仓库发布仍未完成。
