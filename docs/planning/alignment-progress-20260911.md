# 对齐工程进度与恢复点

更新时间：2026-09-11

- [x] 恢复最新 checkpoint、读取用户引用任务、检查工作副本与上轮补丁。
- [x] 用户澄清为 Codex 内置浏览器，使用 cua.createBrowserTab("iab", ...) 已成功读取登录后画布；外部枚举认证错误不代表内置浏览器不可用。
- [x] 原生组件、存储、布局与 Nova 多场景能力审查。
- [x] 冷保存实施计划与多维度对抗审查。
- [x] 交互生命周期与可测菜单定位已编写并回归通过。
- [x] 创建栏、项目区、按需面板和移动端布局。
- [x] 原生多工作画布与整组归档。
- [x] 自动化回归、截图找茬、构建与进度收尾。

当前服务：`127.0.0.1:3011`，监听 loopback；Playwright 与页面巡检均复用该服务。
禁止用 docs/planning/kachang-reference-1440.png 作为卡藏编辑器证据，它是登录页。
本轮已完成“无限画布底座交互、卡藏式工作区层级和多工作画布归档”范围。
后续 GenerationRun、资产版本、分镜交付、剪辑交付和 Agnes 视频协议不在本轮完成声明中。

## 继续执行恢复点

- [x] 复现 Playwright 全套回归失败：8 项均卡在 `snapshot()` 读取空 store。
- [x] 定位为 Vite HMR 带时间戳模块与无查询动态导入形成不同模块实例，不是画布创建或持久化逻辑失效。
- [x] 让 Playwright 从页面已加载资源中复用 store 模块 URL，重新验证 10 项回归。
- [x] 继续逐页检查主页、画布库、图片、视频、素材、提示词和配置页面。
- [x] 完成卡藏公开可观察能力的下一批本地落地：画布外观设置、默认画幅、连线显示、社区卡片入口。
- [x] 修复主页快速开始说明中的旧伪 HTML 标记，并补充主页入口回归测试。

## 登录后实察更新

- 内置浏览器 tab 1 已观察登录画布，浏览器 ID 1 / iab。优先 getTab("1", {browser:"iab"})，不要改用外部 Chrome。
- 页面分成顶部项目与设置、左侧创建栏、底部编辑栏、左下缩放。
- 通过创建菜单添加了一个空图片节点，观察到下方提示词编辑器、模型、生成张数、画面比例、分辨率、质量与生成按钮。
- 未输入私有提示词，未上传素材，未提交生成，未修改账号/积分。
- 资源库包含卡片库/资产库、分组、搜索、分页；实察存在资源库与节点编辑器同时展开相互遮挡的情况，本地不照搬这一缺陷。
- 未删除刚创建的空图片节点，仍在该卡藏画布中。

## 本轮最终证据

- 本地服务：`http://127.0.0.1:3011/canvas`；Playwright 与页面巡检复用该服务，未关闭用户已有浏览器标签页。
- `npm run typecheck`：通过。
- `npm run build`：通过；仅保留既有的大包和动态导入提示。
- `npm test`：10/10 通过。
- `git diff --check`：通过；仅有 Git 对换行格式的提示。
- Playwright 覆盖：立即创建、右键创建、右键拖动、正反向连线、重复连线、ESC/指针取消、刷新恢复、工作画布隔离、复制 ID 重映射、整组导入导出、主页提示词社区入口、1440px、390px 和短高度移动端。
- 截图：`alignment-canvas-1440x960.png`、`alignment-canvas-390x844.png`、`alignment-canvas-390x568.png` 及对应编辑器截图。
- 未验证：真实 Provider 成功生成、真实积分消耗、Agnes 视频任务轮询协议；不得据此声称端到端生成已完成。

## 后续阶段恢复入口

图片工作台任务闭环阶段已完成，最新恢复点为 `image-run-lifecycle-progress-20260911.md`，验收为 `image-run-lifecycle-acceptance-20260911.md`。
该阶段新增持久化图片任务、冻结参数重试、跨页恢复与全站任务入口，最终 24/24 浏览器回归通过。
不要把图片工作台阶段等同于画布执行器或视频任务已迁移；这两项仍是下一阶段重点。
