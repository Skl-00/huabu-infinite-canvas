# Agnes 阶段恢复点

- [x] 恢复主仓库、关联任务及图片任务阶段进度，保留全部未提交改动。
- [x] 官方三模型、视频公共协议和现价文档已实际读取。
- [x] 计划和多维度反方审查已保存。
- [x] 显式 Agnes 渠道及三模型预设，图片生成/编辑专用 JSON，视频 video_id 查询。
- [x] 视频原渠道身份、恢复查询、提交前保存与素材确认已实现。
- [x] 初轮受控协议/恢复回归 7/7，通过后继续补真实返回结构回归。
- [x] 三模型串行真实实测，图片及两段视频真正下载并持久化。
- [x] 真实调用使用独立 Chromium profile；常用内置浏览器配置仍需用户填入密钥。
- [x] 初轮完整回归 34/34，新增手机明暗视口用例定向 1/1 通过。
- [x] 最终修正：预设保留用户模型/脚本/非官方端点，未知视频元数据不显示默认值，修正参考图空态文案。
- [x] 最终 Web 类型检查通过；35/35 完整浏览器回归通过，单 worker、无自动重试。
- [x] 非忽略源码文件凭据模式扫描仅命中既有脱敏测试假密钥，未发现真实密钥。
- [x] 最终 Web 与 Docs 构建、截图复核和验收文档收尾；构建完成证据已在 `agnes-capability-alignment-progress-20260912.md` 最终审计中核对，补齐原遗漏状态。

开发服务器：127.0.0.1:3011，已有服务，未重启。
内置浏览器工具认证失败；原生控制因无法可靠确认网址而停止，不重试或绕过此边界。
密钥未写入仓库。真实文本 Responses 成功；图片 1024x1024、1179620 bytes 已本地保存。
真实视频总计 2 次 POST，两个不同样例：1:1 的 960x960 / 5.166667 秒，以及 16:9 的 1280x720 / 4.48 秒。第一次查询中断后继续原任务，没有重新提交该任务。实际 pending / 顶层 url 已补适配。
视频媒体 HTTP 200 / video/mp4，但无 CORS 响应头，直连首次仅保留远程结果。仓库现有 canvas-proxy 已启动，127.0.0.1:23210，PID 22952；使用本地保存动作后两段视频真正落库，刷新播放及加入素材已通过。
独立 Chromium 验收配置目录：D:\codex\.local\agnes-qa-browser（含本机配置，不进仓库）。常用浏览器未配置：原生控制因无法确认网址而停止，不能绕过限制。用户只需在配置的 Agnes Flash 预设中填写 API Key；此边界必须在最终说明中明确。

恢复审查已重新读取计划、验收、源码差异及 `.local/agnes-validation-20260911/acceptance.json`，确认不回到旧 HuaBu/HuaBuNova，不覆盖此前图片持久任务改动。官方 Markdown 再次直读；网页搜索工具未返回内容，不重复已失败的阅读后端。
